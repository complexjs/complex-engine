cx.Behaviour = Class.extend({
    entity : null,
    initialized : false,
    init : function ( ) {

    },

    setup : function ( entity ) {
        this.entity = entity;

        this.initialized = true;
    },

    update : function ( ) {

    }
});